import React from 'react'

const Display = ( {current} ) => {
    return (
        <div id="display" className="display">
            <h2>{current}</h2>
        </div>
    )
}

export default Display
